[build]
rustflags = ["-C", "target-cpu=native"]

# Uncomment the following lines to build fully static musl binaries:
#rustflags = ["-C", "target-feature=+crt-static", "-C", "target-cpu=native"]
#target = "x86_64-unknown-linux-musl"

[profile.release]
debug = true
